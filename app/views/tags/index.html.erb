
<table class="table "style="width:100%">
  <tr>
    <th>Tag Names</th>
  </tr>
  <tr>
    <% @tags.each do |t|%>
    <td><%= link_to "#{t.name.upcase}",tag_path(t.id), :class => "btn"%></td>
  </tr>
    <% end %>
</table>